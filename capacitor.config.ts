import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'shell-component-ion-back-button-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
